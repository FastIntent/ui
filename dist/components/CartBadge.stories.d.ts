import type { Meta, StoryObj } from '@storybook/react';
import { CartBadge } from './CartBadge/CartBadge';
declare const meta: Meta<typeof CartBadge>;
export default meta;
type Story = StoryObj<typeof CartBadge>;
export declare const Default: Story;
